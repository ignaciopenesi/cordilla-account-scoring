.venv/
__pycache__/
.ipynb_checkpoints/
*.pyc

# local working notes, not part of the submission
_notes/

# simulated cycle memory from --demo runs; check.sh removes it, a manual run may not
serving/out/cycles.jsonl
serving/out/next_cycle.json
